Screenshots referenced by the README and forum post go here.

The Gallery style guide expects an image directly below the Usage line.
See https://alfred.app/submit/screenshots/ for their guidance.

otpadd.png — Alfred showing the `otpadd` keyword.

Still worth adding: a shot of the `otp` keyword listing accounts. Store a
throwaway seed first, so the screenshot does not publish a real account name.
